workspace = "boringcache/benchmark-storybook"

[adapters.nx]
tag = "storybook-nx-local"
no-platform = true
no-git = true
command = ["corepack", "yarn", "nx", "run", "bench/react-vite-default-ts:build", "--parallel=1"]
