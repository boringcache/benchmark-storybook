name: "Storybook Nx Fresh"
run-name: >-
  ${{ format('Benchmark {0} | {1}', github.event_name == 'pull_request' && 'Validation' || inputs.cli_version != '' && 'Canary' || 'Fresh', github.event_name == 'schedule' && 'Weekly' || github.event_name == 'pull_request' && format('PR #{0}', github.event.pull_request.number) || format('Manual dispatch ({0})', github.ref_name)) }}

on:
  workflow_dispatch:
    inputs:
      cli_version:
        description: "Optional exact BoringCache CLI release tag."
        required: false
        type: string
        default: ""
      runner_label:
        description: "Optional runner label for a manual main-branch experiment."
        required: false
        type: string
        default: ""
  schedule:
    - cron: "0 4 * * 3"
  pull_request:
    paths:
      - ".gitmodules"
      - ".github/actions/storybook-nx-benchmark/**"
      - ".github/workflows/storybook-benchmark.yml"
      - ".github/workflows/storybook-fresh-benchmark.yml"
      - "README.md"
      - "scenarios/**"
      - "scripts/**"
      - "upstream"

permissions:
  contents: read
  actions: write

env:
  BORINGCACHE_RESTORE_TOKEN: ${{ secrets.BORINGCACHE_RESTORE_TOKEN }}
  BORINGCACHE_SAVE_TOKEN: ${{ secrets.BORINGCACHE_SAVE_TOKEN }}
  BORINGCACHE_NO_GIT: "true"

jobs:
  cold:
    name: ${{ matrix.provider }} Storybook cold
    if: inputs.runner_label == '' || github.ref_name == 'main'
    runs-on: ${{ github.event_name == 'workflow_dispatch' && github.ref_name == 'main' && inputs.runner_label || 'ubuntu-latest' }}
    timeout-minutes: 180
    strategy:
      fail-fast: false
      matrix:
        include:
          - provider: GitHub Actions
            strategy: actions-cache
          - provider: BoringCache
            strategy: boringcache
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/storybook-nx-benchmark
        with:
          strategy: ${{ matrix.strategy }}
          cache_lane: fresh
          phase: publish
          cli_version: ${{ inputs.cli_version }}
          node_version: "22.22.3"

  warm:
    name: ${{ matrix.provider }} Storybook warm
    needs: cold
    runs-on: ${{ github.event_name == 'workflow_dispatch' && github.ref_name == 'main' && inputs.runner_label || 'ubuntu-latest' }}
    timeout-minutes: 180
    strategy:
      fail-fast: false
      matrix:
        include:
          - provider: GitHub Actions
            strategy: actions-cache
          - provider: BoringCache
            strategy: boringcache
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/storybook-nx-benchmark
        with:
          strategy: ${{ matrix.strategy }}
          cache_lane: fresh
          phase: warm
          cli_version: ${{ inputs.cli_version }}
          node_version: "22.22.3"
